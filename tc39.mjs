// File extension .mjs (ESM -> EcmaScriptModules)

import "core-js/proposals/change-array-by-copy.js"; //need the file extension at the end

const number = [1, 2, 3].toReversed();
console.log(number);
